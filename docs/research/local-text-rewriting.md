# Local Text Rewriting

Future research topic: Ollama behind a replaceable abstraction.

Evaluation dimensions:

- Meaning preservation.
- No hallucination.
- Preservation of numbers, names, URLs, commands, paths, versions, and identifiers.
- Deterministic fallback when validation fails.

No rewriting implementation exists in Phase 00.
